{
  "name": "uswds-mayflower",
  "version": "1.0.0",
  "scripts": {
    "build": "postcss styles/**/[!_]*.scss -d build --ext css",
    "start": "npm run watch",
    "prestart": "npm run build",
    "watch": "chokidar \"styles/**/*.scss\" -c \"npm run build\""
  },
  "dependencies": {
    "uswds": "^2.4.1"
  },
  "devDependencies": {
    "@csstools/postcss-sass": "^4.0.0",
    "autoprefixer": "^9.7.4",
    "chokidar-cli": "^2.1.0",
    "postcss-cli": "^7.1.0",
    "postcss-scss": "^2.0.0"
  }
}
